# conda_env.yml
# Configuration file for creating a Conda Environment with dependencies needed for Gaia.
# Create the environment by running the following command (after installing Miniconda):
#   $ conda env create -f conda_env.yml

name: gaia

channels:
- conda-forge
- defaults

dependencies:
- numpy==1.10.4
- six
- requests
- shapely
- geopandas
- pillow
- gdal
- celery
- psycopg2
- geoalchemy2